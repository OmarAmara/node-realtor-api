// Auth to determine if user hitting route is either loggedIn client or if user is client's Realtor?

//.get('/:clientId')
if(req.session.loggedInUser._id === clientId)


// or 

if(req.sessions.loggedInUser.)id !== clientId) {
	// check to see if this logged in user is the Client's realtor instead...


	// check to see if this person is een a client...
}

// continue terminate route first, then work on this after search model/ controller is completed.